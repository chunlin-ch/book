---
tags:
  - 概率论
  - 第三章
  - 理论方法
难度: 中级
前置知识:
  - "[[070-理论方法-母函数的性质]]"
---

# 知识点概述
利用母函数（矩母函数MGF或概率母函数PGF）是求解独立随机变量之和的分布的系统性方法。其核心原理在于，独立随机变量和的母函数等于各个变量母函数的乘积。这个性质将复杂的卷积运算转化为了简单的乘法运算，极大地简化了问题。

# 教材原文
*(该方法是母函数性质的直接应用，是标准概率论内容。)*

# 详细解释

### 核心原理

设 $X_1, X_2, \dots, X_n$ 是 **相互独立** 的随机变量，令 $S_n = X_1 + X_2 + \dots + X_n$。

*   **对于矩母函数 (MGF)**:
    $$M_{S_n}(t) = M_{X_1}(t) \cdot M_{X_2}(t) \cdots M_{X_n}(t)$$

*   **对于概率母函数 (PGF)** (当变量为非负整数时):
    $$G_{S_n}(s) = G_{X_1}(s) \cdot G_{X_2}(s) \cdots G_{X_n}(s)$$

### 求解步骤

1.  **识别独立和**: 确认所求的随机变量可以表示为一系列独立随机变量的和。
2.  **求单个母函数**: 求出单个随机变量 $X_i$ 的母函数（MGF或PGF）。
3.  **计算乘积**: 将所有单个母函数相乘，得到和 $S_n$ 的母函数。
4.  **识别分布 (逆向工程)**: 观察和的母函数的形式，并将其与已知分布的母函数形式进行匹配。根据[[070-理论方法-母函数的性质|母函数的唯一性]]，如果形式匹配，就可以确定和的分布。

# 学习要点
*   **前提是独立**: 再次强调，此方法仅适用于**相互独立**的随机变量之和。
*   **化卷积为乘积**: 这是母函数方法最根本的优势。
*   **熟悉常见分布的母函数**: 为了能够完成最后一步“识别分布”，必须熟记常见分布（二项、泊松、正态、伽玛、指数等）的母函数形式。

# 实践应用

### 例题1: 证明二项分布是伯努利分布的和

设 $X_i$ ($i=1, \dots, n$) 是独立同分布的伯努利随机变量， $X_i \sim B(1, p)$。求 $S_n = \sum_{i=1}^n X_i$ 的分布。

**解题思路**:
1.  **单个PGF**: 伯努利分布 $X_i$ 的PGF为：
    $G_{X_i}(s) = E(s^{X_i}) = s^1 P(X_i=1) + s^0 P(X_i=0) = ps + (1-p)$。
2.  **计算乘积**: 由于所有 $X_i$ 独立同分布，和的PGF为：
    $G_{S_n}(s) = \prod_{i=1}^n G_{X_i}(s) = [ps + (1-p)]^n$。
3.  **识别分布**: 我们发现 $G_{S_n}(s)$ 的形式正是二项分布 $B(n,p)$ 的概率母函数。根据唯一性，可以断定 $S_n \sim B(n,p)$。
    *验证：二项分布的PGF为 $\sum_{k=0}^n s^k \binom{n}{k}p^k(1-p)^{n-k} = \sum_{k=0}^n \binom{n}{k}(sp)^k(1-p)^{n-k} = (sp + 1-p)^n$。匹配成功。

### 例题2: 证明正态分布的可加性（再生性）

设 $X \sim N(\mu_1, \sigma_1^2)$ 和 $Y \sim N(\mu_2, \sigma_2^2)$ 相互独立。求 $Z=X+Y$ 的分布。

**解题思路**:
1.  **单个MGF**: 正态分布 $N(\mu, \sigma^2)$ 的MGF为 $M(t) = e^{\mu t + \frac{1}{2}\sigma^2 t^2}$。
    $M_X(t) = e^{\mu_1 t + \frac{1}{2}\sigma_1^2 t^2}$
    $M_Y(t) = e^{\mu_2 t + \frac{1}{2}\sigma_2^2 t^2}$
2.  **计算乘积**: 
    $M_Z(t) = M_X(t) M_Y(t) = e^{\mu_1 t + \frac{1}{2}\sigma_1^2 t^2} \cdot e^{\mu_2 t + \frac{1}{2}\sigma_2^2 t^2} = e^{(\mu_1+\mu_2)t + \frac{1}{2}(\sigma_1^2+\sigma_2^2)t^2}$。
3.  **识别分布**: 我们发现 $Z$ 的MGF仍然是正态分布的MGF形式，其均值参数为 $\mu_1+\mu_2$，方差参数为 $\sigma_1^2+\sigma_2^2$。
    根据唯一性， $Z \sim N(\mu_1+\mu_2, \sigma_1^2+\sigma_2^2)$。

# 关联知识点
*   **前置知识**:
    *   [[070-理论方法-母函数的性质]]
*   **核心应用**:
    *   [[075-核心概念-分布的再生性]]
*   **对比方法**:
    *   [[056-理论方法-卷积公式]] (母函数法通常比卷积法更简单)
